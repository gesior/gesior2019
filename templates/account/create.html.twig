{% extends 'base.html.twig' %}

{% block title %}{{ 'ACCOUNT.CREATE.TITLE'|trans }}{% endblock %}

{% block body %}
    <a href="{{ url('account_index') }}" class="btn btn-sm btn-warning">
        {{ 'Back'|trans }}
    </a>

    <h2>{{ 'ACCOUNT.CREATE.TITLE'|trans }}</h2>

    {{ form(form) }}

{% endblock %}
